node_modules/
dist/
dev-dist/
coverage/
playwright-report/
test-results/
blob-report/
package-lock.json

# 프로젝트 문서는 손으로 쓴 한국어 산문 + 표다.
# prettier 가 표 정렬과 줄바꿈을 다시 흘려서 헌법 문서에 의미 없는 diff 를 만든다.
# format:check 게이트의 대상은 코드(ts/js/json/html/css)다.
CLAUDE.md
PROGRESS.md
DECISIONS.md
.claude/

# TWA 안드로이드 프로젝트: bubblewrap 이 생성·관리한다 (twa-manifest.json 의 포맷 포함).
# 우리 손으로 고치는 파일이 아니므로 포맷 게이트의 대상이 아니다.
twa/

# 릴리즈 절차 문서도 위 헌법 문서들과 같은 한국어 산문이다.
docs/
