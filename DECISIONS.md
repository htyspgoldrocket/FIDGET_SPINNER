# DECISIONS.md — 아키텍처 결정 기록 (ADR)

> 설계 판단이 생기거나 바뀔 때마다 여기에 추가한다. **기존 항목은 수정하지 않고**, 뒤집힐 경우 새 ADR을 추가하고 이전 것을 `Superseded`로 표시한다.
> `CLAUDE.md`의 규칙을 어겨야 한다고 판단되면, 코드를 고치기 전에 여기에 ADR을 먼저 쓰고 승인을 받는다.

---

## ADR-001 — iOS를 지원 대상에서 제외한다
**날짜**: 2026-08-04 · **상태**: Accepted

**배경**: 이 앱의 핵심 가치는 "회전이 손끝에 진동으로 전달되는 감각"이다.

**사실 확인**
- Apple은 WebKit에 Web Vibration API를 한 번도 구현하지 않았다.
- 그동안 쓰이던 우회 기법(`<input type="checkbox" switch>`의 시스템 햅틱을 프로그래밍 방식으로 트리거)은 iOS 17.4~26.4에서만 동작했고 **iOS 26.5에서 Apple이 차단했다.**
- Firefox는 129 버전부터 Vibration API 지원을 제거했다.

**결정**: Android Chrome / Samsung Internet만 정식 지원한다. iOS·Firefox 접속 시 안내 배너를 띄우고 시각·오디오 폴백으로만 동작시킨다.

**대안과 기각 사유**
- Capacitor로 iOS 네이티브 래핑: 진짜 Taptic Engine을 쓸 수 있으나, Apple Developer Program 연회비와 App Store 심사 가이드라인 4.2(최소한의 기능) 반려 리스크가 있다. 웹에서 재미가 검증된 뒤로 미룬다.
- iOS 우회 꼼수 사용: OS 업데이트 한 번에 깨지는 기반 위에 핵심 기능을 올릴 수 없다.

---

## ADR-002 — 연속 진동이 아니라 "디텐트 펄스" 방식을 쓴다
**날짜**: 2026-08-04 · **상태**: Accepted

**배경**: "돌아가는 동안 계속 진동"이 직관적으로 보이지만 잘못된 설계다.

**결정**: 회전각이 `2π / DETENT_COUNT` 경계를 넘을 때마다 짧은 펄스를 1회 발사한다. ω가 빠르면 촘촘하고, 감속하면 벌어진다.

**근거**
1. Web Vibration API에는 **강도(amplitude) 제어가 없다.** ON/OFF 시간만 지정 가능하므로 "부드럽게 도는 느낌"은 물리적으로 표현 불가능하다.
2. 실제 베어링 스피너의 촉감은 연속 진동이 아니라 회전 주기에 맞춘 미세한 걸림이다.
3. 연속 진동은 배터리 소모·발열·진동 모터 수명에 모두 불리하다.
4. 매 프레임 `vibrate()`를 호출하면 이전 패턴을 덮어써서 오히려 감각이 뭉개진다.

**필수 부속 결정**: `MIN_PULSE_GAP_MS = 25` 가드를 반드시 둔다. 없으면 고속 회전 시 진동 큐가 밀려 연속 진동으로 뭉개진다.

---

## ADR-003 — 물리 코어를 브라우저 API에서 완전히 분리한다
**날짜**: 2026-08-04 · **상태**: Accepted

**결정**: `src/core/**`는 `window`/`document`/`navigator`/`Date.now`/`Math.random`을 참조하지 않는다. 시간과 난수는 인자로 주입한다. ESLint 규칙으로 강제한다.

**근거**: 고정 타임스텝 + 순수 함수 조합이라야 **동일 입력 → 동일 출력**이 보장되고, 감속 곡선을 골든 스냅샷으로 회귀 테스트할 수 있다. 촉감은 자동 검증이 불가능하므로, 최소한 그 아래 물리 레이어만큼은 완전히 자동 검증되어야 한다.

**비용**: 의존성 주입 보일러플레이트가 늘어난다. 감수할 만한 대가로 판단한다.

---

## ADR-004 — 기록은 로컬 저장하되, 저장 계층을 추상화한다
**날짜**: 2026-08-04 · **상태**: Accepted

**결정**: `StorageAdapter` 인터페이스 뒤에 IndexedDB 구현체를 둔다. 모든 레코드에 UUID·타임스탬프·`schemaVersion`을 부여한다.

**근거**
- 온라인 리더보드는 매력적이지만 **클라이언트가 보내는 RPM 값을 서버가 검증할 방법이 사실상 없다.** 리더보드는 반드시 조작된다. 이 규모에서 서버 측 물리 재현 검증은 과하다.
- 개인정보처리방침 작성 의무, 백엔드 운영 비용도 1차 범위에 맞지 않는다.
- 다만 나중에 붙일 수 있는 형태로 만들어두면 그때 저장 코드를 갈아엎지 않아도 된다.

**부속 결정**: 브라우저 캐시 삭제로 기록이 통째로 날아가므로, **백업 코드 내보내기/불러오기를 MVP 필수 항목**으로 둔다.

---

## ADR-005 — 자이로(기기 기울임) 조작을 넣지 않는다
**날짜**: 2026-08-04 · **상태**: Accepted

**결정**: 조작은 플릭 / 브레이크 / 더블탭 정지 3종으로 한정한다.

**근거**
1. DeviceMotion 입력은 결정론적 자동 테스트가 불가능해, 풀 하네스 원칙과 정면으로 충돌한다.
2. 기록형 게임에서 기울임이 회전에 영향을 주면 "폰을 흔들어 기록 뻥튀기"가 가능해져 기록의 의미가 사라진다.

---

## ADR-006 — 웹 PWA를 먼저 배포하고 Play Store는 옵션으로 둔다
**날짜**: 2026-08-04 · **상태**: Accepted

**결정**: 1차는 웹 PWA(Vercel). Play Store는 TWA로 나중에 감싼다. 단, TWA 호환 제약(매니페스트 완비 / 오프라인 100% / History API 백버튼 대응 / assetlinks 자리 확보 / keystore 백업)은 **처음부터 지킨다.**

**근거**
- 2023-11-13 이후 생성된 개인 개발자 계정은 **12명 이상의 테스터가 14일 이상 연속 참여한 비공개 테스트**를 마쳐야 프로덕션 액세스를 신청할 수 있고, 신청 후 심사도 통상 7일이 걸린다. 즉 스토어 출시는 최소 3주짜리 일정이다.
- TWA 내부는 동일한 웹앱이므로 진동 동작에 차이가 없다. 기술적으로 나중에 감싸도 손해가 없다.
- 반대로 스토어에 올리는 순간 매년 target SDK 갱신 재빌드 의무가 생긴다. 재미가 검증되기 전에 떠안을 부담이 아니다.

**부속 결정**: 패키지명 `kr.goldrocket.fidgetspinner`는 스토어 등록 시 영구 고정되므로 **지금 확정한다.** 표시명 `FIDGET SPINNER`는 일반명사라 스토어 검색 노출에 불리하므로, 스토어 등재명은 Phase 7에서 별도로 정한다.
